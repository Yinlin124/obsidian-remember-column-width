export const CSS_VAR_NAME = "--file-line-width";

export const DATA_ATTR_MANAGED = "data-rcw-managed";

export const STATUS_BAR_CLS = "rcw-status-bar";
export const STATUS_BAR_LABEL_CLS = "rcw-status-bar-label";
export const STATUS_BAR_SLIDER_CLS = "rcw-status-bar-slider";

export const SAVE_DEBOUNCE_MS = 300;

export const DEFAULTS = {
	globalDefault: 780,
	minWidth: 500,
	maxWidth: 1400,
	enabled: true,
} as const;
